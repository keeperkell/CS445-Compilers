## C-S22
## testing static variables

int goad:666;
## load() is in global, int load is in local but statically allocated
load()
 begin 
    static int load;
     begin 
        static int goad:777;
        output(goad);
     end 
    output(goad);
    outnl();
 end 

main()
 begin 
    load();
 end 
