## C-S22
## recursive fibonacci function (two way recursion)
int fib(int n)
 begin 
        if n<3 then  return 1;
        else return fib(n-1) + fib(n-2);
 end 

main()
 begin 
        int n;

        n <- fib(12);
        output(n);
        outnl();
 end 


