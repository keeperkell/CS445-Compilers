## C-S22
char x;
char y;
char z;

main()
 begin 
    x <- 'A';
    y <- 'B';
    outputc(x);
    outputc(y);
    outputc(z <- x <- y);
    outputc(x);
    outputc(y);
    outputc(z);
    outnl();
 end 
